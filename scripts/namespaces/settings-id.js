/**
 * Contains the HTML IDs of the settings.html elements, to make sure they're consistent.
 * Also contains selector-container which is a class not an ID but I put it here anyways for convenience.
 */
const SettingsID = {
  reconnectBtn: "reconnect-button",
  statusMsg: "status-message",
  selectorContainerClass: "selector-container", //actually a classname not an ID
  modelField: "model-field",
  imageField: "image-field",
  audioField: "audio-field",
  imageShortcutField: "image-shortcut-field",
  audioShortcutField: "audio-shortcut-field",
};
export default SettingsID;
